<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Admin - ExtraEstetica</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/global.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="py-5">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center card-custom admin-header mb-4">
            <h3>🛡️ Administración</h3>
            <button onclick="logout()" class="btn btn-outline-danger">Cerrar Sesión</button>
        </div>

        <div class="row g-4">
            <div class="col-md-4">
                <div class="card-custom p-4 h-100">
                    <h5>👤 Crear Empleado</h5>
                    <form action="/admin/create-employee" method="POST">
                        <input type="text" name="nombre" class="form-control mb-2" placeholder="Nombre" required>
                        <input type="email" name="email" class="form-control mb-2" placeholder="Email" required>
                        <input type="password" name="password" class="form-control mb-2" placeholder="Password" required>
                        <select name="role" class="form-select mb-3">
                            <option value="ESTILISTA">Estilista</option>
                            <option value="MANICURISTA">Manicurista</option>
                        </select>
                        <button type="submit" class="btn btn-militar w-100">Registrar</button>
                    </form>
                </div>
            </div>

            <div class="col-md-8">
                <div class="card-custom p-4 h-100">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                         <h5 class="fw-bold mb-0">📅 Agenda</h5>
                         
                         <form action="/admin" method="GET" class="d-flex gap-2">
                             <input type="date" name="fecha" class="form-control form-control-sm" 
                                    value="<%= fechaSeleccionada %>" required>
                             <button type="submit" class="btn btn-sm btn-militar">Buscar</button>
                         </form>
                    </div>

                    <div class="table-responsive" style="max-height: 400px; overflow-y: auto;">
                        <table class="table table-hover align-middle">
                            <thead class="table-light sticky-top">
                                <tr>
                                    <th>Hora</th>
                                    <th>Cliente</th>
                                    <th>Servicio</th>
                                    <th>Atiende</th>
                                    <th>Acción</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (appointments && appointments.length > 0) { %>
                                    <% appointments.forEach(cita => { %>
                                        <tr>
                                            <td>
                                                <div class="fw-bold fs-5"><%= new Date(cita.fecha_hora_inicio).toLocaleTimeString('es-MX', {hour: '2-digit', minute:'2-digit'}) %></div>
                                            </td>
                                            
                                            <td><%= cita.client ? cita.client.nombre_completo : 'Anónimo' %></td>
                                            
                                            <td><span class="badge bg-light text-dark border"><%= cita.service ? cita.service.nombre : '?' %></span></td>
                                            
                                            <td class="fw-bold text-success">
                                                <%= (cita.employee && cita.employee.User) ? cita.employee.User.nombre_completo : 
                                                    (cita.employee && cita.employee.user) ? cita.employee.user.nombre_completo : 'Sin Asignar' %>
                                            </td>
                                            
                                            <td>
                                                <% if(cita.estado !== 'cancelada') { %>
                                                    <form action="/admin/cancel-appointment/<%= cita.id %>" method="POST" style="display:inline;">
                                                        <button type="submit" class="btn btn-sm btn-outline-danger" title="Cancelar Cita">✕</button>
                                                    </form>
                                                <% } else { %>
                                                    <span class="badge bg-danger">Cancelada</span>
                                                <% } %>
                                            </td>
                                        </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="5" class="text-center py-5">
                                            <h4 class="text-muted">No hay citas para esta fecha 🏝️</h4>
                                            <small>Intenta buscar otro día en el calendario de arriba</small>
                                        </td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            <div class="col-12">
                <div class="card-custom p-4">
                    <h5 class="fw-bold" style="color: var(--color-militar);">💰 Gestión de Precios</h5>
                    <div class="table-responsive">
                        <table class="table align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>Servicio</th>
                                    <th>Precio Actual</th>
                                    <th>Nuevo Precio</th>
                                    <th>Acción</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (services && services.length > 0) { %>
                                    <% services.forEach(service => { %>
                                        <tr>
                                            <form action="/admin/update-price" method="POST">
                                                <input type="hidden" name="id" value="<%= service.id %>">
                                                <td class="fw-bold"><%= service.nombre %></td>
                                                <td>$<%= service.precio %></td>
                                                <td>
                                                    <div class="input-group input-group-sm">
                                                        <span class="input-group-text">$</span>
                                                        <input type="number" name="precio" class="form-control" 
       value="<%= service.precio %>" step="0.01" min="0" required 
       style="max-width: 100px;">
                                                    </div>
                                                </td>
                                                <td>
                                                    <button type="submit" class="btn btn-sm btn-success">Guardar</button>
                                                </td>
                                            </form>
                                        </tr>
                                    <% }) %>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        function logout() {
            localStorage.clear();
            window.location.href = '/';
        }
    </script>
</body>
</html>